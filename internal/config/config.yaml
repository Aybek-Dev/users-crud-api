# database settings
database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: postgres
  sslmode: disable

#server settings
server:
  port: 8080
  read_timeout: 5s
  write_timeout: 5s

# environment settings
env: development # production